# Autograde 开发文档

> 本文档是 Autograde 的**单一事实来源（Single Source of Truth）**。
> 记录已确认的产品决策、数据模型草案与里程碑规划，供跨会话、跨 AI 协作交接使用。
> 产品细节未定稿前，本文档持续进化；定稿后再最终敲定技术实现细节。
>
> 工作代号：**Autograde**（正式命名待定，可能沿用天文代号体系）

---

## 一、项目定位

**一句话：以班级和作业批次为中心的批改工作台——客观题零 AI 自动拼装，批改过程自动沉淀数据资产。**

- **前身**：Gradify（英语作业反馈生成器，模板拼接 + AI 扩写的一次性文本装配器）
- **使用场景**：碳碳的厚海原版英语助教兼职（两个班，级别/班号/上课时间不同，每班每周一次课）
- **重写而非改旧版的理由**：
  - 旧 Gradify 在每周生产使用中，不能边飞边换引擎
  - 新功能的数据模型与交互复杂度已超出旧版单文件架构
  - 顺带解决旧版屎山问题

### 核心设计原则（贯穿所有决策）

1. **AI 一次性加工，人工验收冻结，之后零 AI**——录入答案/赋分时 AI 做解析器与建议者，人确认后冻结入库；AI 永不接触已冻结的原文
2. **确定性任务零 AI**——查表、拼装、算分、排版全部是数据库 lookup + 模板渲染 + 纯数学，毫秒级、零成本、100% 忠实原文
3. **AI 只做生成与分析，且生成永远是起草**——讲解由「老师描述错点 → AI 起草 → 人工修改定稿」；薄弱点归纳这类分析才是 AI 的主场
4. **一切皆为数据**——话术、题目、解析、反馈文本全部入库，无一硬编码
5. **先定设计令牌，再写组件**——UI 不允许滑向「AI 默认皮肤」，所有页面只能在设计系统约束上生长

---

## 二、已确认的产品决策

### 2.1 核心概念模型

```
班级 Class（两个班：级别/班号/上课时间不同）
 ├── 学生 Student（属于班级，选班级自动带出名单）
 └── 作业批次 Assignment（一批作业 = 一个工作空间）
      ├── 基本信息：上课时间 / 第几次课 / 作业内容
      └── 题目 Question（题号/题型/标准答案/解析原文/板块标签）
           └── 错题记录 ErrorRecord（学生 × 题目，每次批改一行）
```

- **班级是核心组织单位**，上课时间决定登记作业时间与批改 deadline
- **班级命名体系（真实规则，禁用「少年班/中级班」示例叫法）**：
  - **厚少**：`WW` + 级别 + 册，如 **WW5A** = Wonderful World（国家地理出版社教材）5 级上册
  - **厚中**：`NG` + 级别 + 册，如 **NG3B** = 厚中 3 级下册
  - **册 = 单元范围**：A = U1-6（上册），B = U7-12（下册）——单元号可直接按册归属校验
- 数据不做物理隔离——所有表挂 `class_id`，班级/个人双尺度只是聚合维度不同
- 两个班的真实形态（决策依据）：
  - **厚少**：练习册，一次作业约 6 个 Task，每个 Task 5-6 题，共 20-40 题
  - **厚中**：答题卡式，客观题为主，题量较少

### 2.2 页面信息架构（四级）

```
工作台 Dashboard（一级：整体状态、待办、Deadline 提醒）
 └── 班级（二级：某班的学生、各次作业、班级统计）
      └── 作业批次（三级：该批次详情与批改进度）
           └── 批改界面（四级：三栏工作台，逐学生批改）
```

- 批改界面**不是首页**；首页是承载整体状态的 Dashboard

### 2.3 创建批改任务的流程

1. **选班级** → 自动带出该班学生名单
2. **填基本信息** → 上课时间、第几次课、作业内容（可先添加项目）
3. **录入答案/题库** → AI 一次性结构化拆分 → **人工过目验收 → 冻结入库**
   - 此入口的摩擦成本是产品成败手，目标：一批题录入压到 2 分钟以内
   - **原始素材形态**：机构 PDF 答案经标准 Word 转换后按章节复制文本粘贴
   - **AI 拆分的额外职责**：识别并剔除混入文本的机构水印等无关内容
   - **答案为图片的题型**（连线题、对话勾选等）：按主观题处理（manual/ai_expand），解析落地为文本，M1 不做视觉解析

### 2.4 题目三种处理模式（录题库时逐题标记）

| 模式 | 适用 | 行为 |
|------|------|------|
| `verbatim` 直出 | 客观题（默认） | 冻结的解析原文直接排版输出，零 AI |
| `ai_expand` AI 扩写 | 造句类（有答案无解析） | 老师先描述学生错点，AI 基于题目+答案+错点起草讲解，可手动修改后定稿 |
| `manual` 人工填充 | 需要老师个性化点评 | 框架全自动搭好，在编辑窗内只填内容 |

- **模式是题目的属性**（录入时定），不是批改时临时想的开关；批改界面保留临时覆盖能力

### 2.5 排版全自动化 + 错题勾选器（消灭旧版最大摩擦点）

- **勾选器形态：板块分组式**（Task 1 / Task 2 … 或 Reading / Vocabulary 分组），组内题号芯片勾选，组级配全选/反选；两个班统一使用
- 勾选错题后，系统自动完成全部排版：
  - 按**板块分组**输出（Reading 部分 / Vocabulary 部分 / Task 编号标题……），该加粗的加粗
  - 题号按勾选自动排好
  - `verbatim` 题：解析原文自动填入
  - `ai_expand` / `manual` 题：留出内容位
- **旧版痛点「手动复制板块标题再粘贴」彻底消灭**，老师只剩「填内容」一个动作

### 2.6 生成区只读，编辑回流 ⭐

- **最终文本区 = 预览 + 复制 + 落库，一个字都不能改**
- 要修改某道题的内容：在批改流程中弹出该题的**统一编辑窗（modal）** → 确认后预览自动重算
- **统一编辑窗按模式分态**：
  - `verbatim` 题：展示冻结解析（只读）
  - `manual` 题：空白编辑框，框架已排好只填内容
  - `ai_expand` 题：**先由老师描述学生错点**（如「I go to park 漏冠词，park 是可数名词」）→ AI 基于题目 + 标准答案 + 错点起草 → **结果在窗内可手动修改** → 确认定稿
- 理由：
  - 数据源只有一处（批改表单状态），预览永远最新
  - 不存在「保存按钮」，不存在「改了忘了存」
  - 最终文本可放心直接落库
  - AI 永远只是起草员，人是定稿者——因材施教的前提是 AI 知道学生错在哪

### 2.7 赋分与等级联动 ⭐

- **AI 赋分预填 + 手动覆盖**：录题库时 AI 建议每题分值权重，预填可改；确认后冻结
- **实时总分**：批改界面学生信息区显示**大数字总分（保留两位小数）**，随错题勾选实时变化（勾得越多扣得越多）
- **等级自动预选**：等级随总分按分数线自动跳档，可**手动覆盖**
- **十档等级**：A+ / A / A- / B+ / B / B- / C+ / C / C- / D
- 默认分数线（**可配置，待碳碳校准**）：A+ ≥97 · A ≥93 · A- ≥90 · B+ ≥85 · B ≥80 · B- ≥75 · C+ ≥70 · C ≥65 · C- ≥60 · D <60

### 2.8 话术库系统

- 旧版问候语/评级话术/Issue 模板**全部迁移入库**，不再硬编码（评级话术需扩展至十档）
- 话术三分类：内置话术 / 自定义话术（可入库复用）/ 单次话术（用完即弃）
- **话术库可视化管理**（增删改查界面）
- **越用越聪明**：记录每条话术使用次数，高频自动排前，个人话术风格持续沉淀

### 2.9 批改进度与看板

- 批改某学生后**自动打勾**，直观看到：谁交了 / 谁没交 / 每人正确率
- **学生状态灯（四态）**：🟢 绿 = 已批改（带分数）｜🟡 黄 = 缺作业（**已交但有缺项，照常批改显示分数**，旁边标「缺项」）｜🔴 红 = 未交（无分数）｜⚪ 空心 = 待批改；灯尺寸适中（10px），一眼可辨
- **未交自动进反馈**：标记未交 → 反馈文本自动带催交话术（旧版 Issue 模板升级版）
- **Deadline 看板**：基于上课时间推算，如「WW5A · U7Day1 还有 3 人未改，距上课 2 天」

### 2.10 多尺度统计（助教个人版「网阅平台」）

- **班级尺度**：平均分、提交率、每题正确率分布（哪些题全班都错 → 下次课该讲什么）、班级间对比
- **个人尺度**：某次作业详情、历次作业趋势

### 2.11 反馈文本全量存档 → AI 薄弱点分析

- 每次生成的最终反馈**全文落库**（纯文本，存储成本极低）
- 积累足够后：一键拉出某学生的薄弱点 / 优势 / 劣势
- AI 分析对象 = 结构化错题记录 + 历史反馈文本，双料喂养

### 2.12 访问控制（暂缓）

- 目前仅碳碳一人使用，**登录体系不着急做**（技术栈成熟，未来要做一次做干净）
- MVP 阶段如部署到公网，用**访问口令**兜底防 AI 成本被刷
- 未来若开放给同事使用，再上完整账号体系（可搬 Stellaris 的 JWT + bcrypt 方案）

### 2.13 学生信息栏（批改界面顶部）

- 批改页顶部是**完整的学生信息栏**，不是孤立的分数卡：
  - 左侧：学生名（衬线大字）+ 班级/批次/课次元信息 + 关键数据（近 5 次平均、上一次得分、出勤）+ **历史薄弱点标签** + **可主动填写的备注**（仅自己可见）
  - 右侧：实时分数预览卡（虚线框，标注「当前分数 · 预览」）
- 数据模型增补：`students` 表加 `note` 字段（老师备注）

---

## 三、数据模型草案

| 表 | 关键字段 | 说明 |
|---|---|---|
| `classes` | name, series(WW/NG), level, term(A/B), schedule | 厚少=WW（Wonderful World）、厚中=NG；A=U1-6 上册，B=U7-12 下册；上课时间驱动 deadline |
| `students` | name, class_id, note | 学生，支持插班/转班；note 为老师备注（仅自己可见） |
| `assignments` | class_id, unit_label, lesson_no, class_time, content, status | 批次 = 工作空间 |
| `questions` | assignment_id, seq, mode(verbatim/ai_expand/manual), section, standard_answer, explanation, score_weight | **解析原文冻结，永不经过 AI**；score_weight 为 AI 预填+人工确认值 |
| `error_records` | student_id, question_id, note, created_at | 数据资产核心；note 存人工填充/AI 定稿内容 |
| `submissions` | student_id, assignment_id, status(待批改/已批改/缺作业/未交), score, rating, rating_override | score/rating 实时计算；rating_override 存手动覆盖 |
| `feedback_snapshots` | student_id, assignment_id, final_text | 最终反馈全文存档 |
| `phrases` | category, content, scope(内置/自定义), use_count | 话术库，use_count 驱动智能排序 |
| `settings` | key, value | 全局配置（如等级分数线阈值，JSON 存储可编辑） |

---

## 四、里程碑

> 版本规划沿用 SemVer + 天文代号体系（minor/major 配新星名，patch 沿用上一星名），正式开工时定名。

- **M1 · 最小闭环**：设计令牌定制 + 四级页面骨架 + 班级/学生/批次/题库 CRUD + AI 辅助录入（含水印剔除、赋分预填）+ 板块分组勾选器 + 三种模式拼装输出（含统一编辑窗）+ 总分/等级实时联动 + 提交状态看板 + 访问口令
  - 验收标准：碳碳用 Autograde 独立完成一个班一整周的真实批改
- **M2 · 体验补齐**：话术库系统（迁移+自定义+可视化管理）、三级复制策略、反馈全文落库
- **M3 · 数据资产**：班级/个人双尺度统计、趋势图、未交催办、Deadline 看板
- **M4 · 智能层**：AI 薄弱点分析（一键学生画像）、学期报告

---

## 五、技术栈意向（待产品定稿后最终确认）

- **前端**：React 18 + Vite + AntD 5（主题 token 全覆盖定制）+ recharts（已定，复用 Stellaris 肌肉记忆）
- **后端**：FastAPI + SQLAlchemy 2.0 + SQLite（aiosqlite）+ `_ensure_columns` 启动自愈（意向，照搬 Stellaris 模式）
- **AI 接入**：OpenAI 兼容协议，网关与模型走环境变量，Mimo / DeepSeek / 其他随时可切换，代码层不绑定
- **部署**：Zeabur；新旧版并行，旧 Gradify 继续服役至 Autograde 稳定一个批改周期

---

## 六、UI 设计方向（令牌 v2 迭代中）

**目标气质：学术沉稳 × 理性冷调**——参考好分数等网阅平台的沉稳，但拒绝过度正式。

**v1 反馈（碳碳）→ v2 修正：**
- ❌ 藏青太深、太端庄 → ✅ **蓝绿（teal/cyan 之间）浅亮配色**
- ❌ 灰色提示小字、英文大写微标签 = AI 味重灾区 → ✅ 全部剔除，标签直接用中文，让排版与留白自己说话
- ❌ 暖白纸感（更早否决）→ ✅ 白底 + 细分隔线，冷暖由蓝绿主题色调节
- ✅ 衬线标题保留（没问题）

**v2 令牌要点：**
- **字体**：标题衬线（Noto Serif SC）；正文 Inter / PingFang SC；分数等关键数字 JetBrains Mono
- **色彩**：白底、蓝黑墨色文字、浅亮蓝绿主题色，语义色降饱和
- **质感**：无渐变、无玻璃拟态、无 emoji 图标、无解释性灰色提示文本
- **文件**：令牌 `design/tokens.css`；概念稿 v2 双页 `design/dashboard.html`（一级工作台）+ `design/grading.html`（四级批改界面）

---

## 七、待定问题清单

- [ ] 正式项目命名（Autograde or 天文系新名）
- [ ] 未来是否开放给同事使用（决定账号体系的引入时机，暂不影响 MVP）
- [ ] 话术分类体系（问候/评级/Issue/板块引导语……具体类目待定）
- [ ] 等级分数线阈值校准（默认版已给，待碳碳按真实分布调整）
- [ ] 设计令牌 v2 验收（双页概念稿反馈 → 迭代）

---

## 八、文档维护约定

- 本文档为单一事实来源：产品决策、数据模型、里程碑以本文档为准
- 参照 Stellaris 惯例：每个版本的开发过程与关键变更落进本文档，发版时同步更新
- 跨会话/跨 AI 交接：新会话先读本文档即可无缝接手

---

## 九、开发日志

### v0.0.1（2026-09-06）· 工程骨架

**交付内容：**

- **backend/**（FastAPI + SQLAlchemy 2.0 + SQLite/aiosqlite）
  - `models.py`：9 张表全量建模，字段以「三、数据模型草案」为准（questions 补 stem 题干列；统一补 created_at）
  - `database.py`：`_ensure_columns` 启动自愈（create_all + 逐表补列，零手动迁移）
  - `main.py`：访问口令中间件（env `ACCESS_TOKEN`，未设置不启用；启用时除 `/api/health` 外所有 `/api/*` 需 Bearer）；CORS 放行 5173
  - `routers/`：`GET /api/health`、`GET /api/classes`（学生数/待批改数/上批平均分）、`GET /api/classes/{id}`（批次+学生）、`GET /api/assignments`（最近批次，工作台用）、`GET /api/assignments/{id}`（按 section 分组的题库）、`GET /api/assignments/{id}/students`（提交四态/分数/等级/已勾选错题/近 5 次平均/上一次/历史薄弱板块）
  - `rating.py`：十档等级默认分数线（2.7），settings 表 `rating_thresholds` 可覆盖
  - `seed.py`：厚少 WW5A（周六 14:00，8 名学生）+ 厚中 NG3B（周四 18:30，6 名学生）；U7Day1 伴学手册（第 7 次课）4 个 Task 共 20 题，verbatim/ai_expand/manual 三种 mode 齐全；提交四态（已批改/缺作业/未交/待批改）均有示例；`--fresh` 重建
- **frontend/**（Vite + React 18 + AntD 5 + react-router-dom）
  - `src/tokens.css`：design/tokens.css 1:1 拷贝；`src/theme.js` 映射 AntD ConfigProvider 主题（colorPrimary #0d9aa7、borderRadius 6、三字体栈）
  - 四级路由页：`/` 工作台（复刻 dashboard.html：待办/班级/最近批次，数据走真实 API）；`/classes/:id`；`/assignments/:id`（提交四态灯 + 题库预览 + 进入批改）；`/grading/:assignmentId`（复刻 grading.html 三栏：左学生名单带四态灯与进度条、中学生信息栏 + 十档等级选择器 + 虚线框大数字预览分 + 板块分组芯片（组级全选/清空）、右只读预览按板块拼装）
  - 算分：总分 100 按 score_weight 归一化，扣勾选错题权重，保留两位小数；等级按 2.7 分数线自动预选、可手动覆盖；勾选状态仅存前端内存
  - vite proxy：`/api` → `localhost:8000`
- **工程化**：git init、`.gitignore`、README（快速开始）、CHANGELOG（0.0.1 开篇）

**已知边界（留给后续版本）：** 无写接口（CRUD/落库待 M1）；统一编辑窗、AI 起草未接；右栏仅「复制全部」（问候语/反馈拆分复制等话术库系统落地后补齐）；学生备注为展示占位，编辑待写接口。

---

*文档由 Kimi 与碳碳协作撰写，随讨论持续更新。最后更新：2026-09-07*
