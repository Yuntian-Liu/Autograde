# CHANGELOG

## 0.15.0 — 2026-09-24

**新功能：AI 能力分析报告（六维雷达 + 细析 + 证据引用）与管理后台「模型调用」监控。**

- 学生详情页新增「能力报告」：AI 通读该生错题记录与历次反馈，产出六维能力雷达图（词汇/语法/拼写/阅读/句型/学习习惯）+ 逐维细析（子标签、分析、逐字引用的证据块）+ 总评与教学建议；AI 只起草、你改完确认才存档；历次报告长期保存，雷达图可叠加对比上一份并自动标出各维度涨跌
- 生成前先看「预估卡片」：数据量、预估 tokens、费用（按当前峰/谷价实时计算）与时长；生成后显示实际消耗，报告页长期可见本次成本与用时
- 管理后台新增「模型调用」：正常率、平均首字延迟、缓存命中率等健康指标（24 小时/7 天）+ 每笔调用明细（业务对象带名字与编码）；点进任意一笔看「消费单」——首字延迟/思考/回答时长、tokens 五项拆分、逐项费用明细与高亮合计（单价为结算时快照，改价不影响旧单）
- DeepSeek 缓存命中纳入观测与结算：命中缓存的输入按 1/8 折扣价计费，消费单单独成行
- 全站版面拓宽分档（表格类页面更宽、阅读类保护行长），页面切换时宽度丝滑过渡；能力雷达图放大呈现

## 0.14.1 — 2026-09-22

**补丁：修复复制到微信图片不显示。**

- 修复：复制到微信后图片丢失——实测定位微信只认 PNG 格式的内联图片（JPEG 会被静默丢弃，体积无关）；现在导出时非 PNG 图片自动无损转码为 PNG 再内联

## 0.14.0 — 2026-09-22

**新功能：笔记一键「复制到微信」；修复全站时间慢 8 小时。**

- 笔记详情页与笔记库列表新增「复制到微信」：加粗/倾斜/高亮格式与图片原样带回微信笔记——图片以微信认的格式内联进剪贴板，整篇一次粘走，备份真正可还原；有图时显示准备进度，个别图拉取失败会提示且不影响其余内容
- 修复：笔记录入时间、管理后台时间等全站时间显示慢 8 小时——后端时间戳是不带时区标记的 UTC 文本，浏览器误当本地时间解析；现在统一按 UTC 解析再转东八区显示；笔记库「创建起止」筛选的临界日错位一并修复
- 备份机制升级：自动备份固定为每天北京时间 04:00 执行（原先跟随部署时刻漂移，每次发版备份时间就变了）；启动时若没有备份或最新备份超 24 小时会先补一份消灭空白期；保留策略改为最近 30 天（按天保留，手动备份多也不会挤掉近期备份）；管理后台备份列表每条带「自动/手动」方式标记和剩余天数（绿 >7 天 / 橙 4-7 天 / 红 ≤3 天，到期显示「今日清理」），并新增「上次备份结果」状态行

## 0.13.0 — 2026-09-21

**新功能：全局业务编码——学生与批次终身唯一的「身份证」。**

- 每个学生、每个批次创建时自动获得终身唯一编码（如 `202603-W51-S00042-7`）：届别（年+学期）· 班型（系列+级别+册）· 类型与序号 · 校验位；同名学生从此一码区分，报障只需报一个码
- 编码展示在班级学生列表、批次详情、学生详情与批改页，点击即复制
- 建班/编辑班级新增「届别」选择（年份 + 春夏秋冬），该班学生与批次的编码前缀随之确定；存量班级已自动回填为 2026 秋季，可修改——只影响之后新发的编码，已发编码终身不变
- 整批导入的学生编码连号；被删学生的编码永久退役、不再复用
- 管理后台新增「编码速查」：输入编码即定位归属者与学生/批次并一键跳转；校验位能识别抄错的编码
- 批量导入改版：每行带编号；填好的行在焦点移到下一行时自动折叠成一行摘要（点击展开，内容不丢），表单不再无限变长；右下角悬浮条随时显示「已填 N 篇」并可直接提交/回顶部/返回；有未导入内容时离开页面会先弹确认

## 0.12.2 — 2026-09-21

**补丁：修复笔记格式「保存后丢失」。**

- 修复：微信笔记粘贴的加粗/倾斜/高亮在录入界面显示正常，但保存后正文变纯文本、一行还被拆成多行——保存链路对顶层格式节点漏判所致；现在保存完整保留格式与换行，阅读态、再编辑往返均不丢（此前已保存的笔记格式在保存时已丢失，无法回溯，介意的话重新粘贴覆盖即可）

## 0.12.1 — 2026-09-19

**补丁：导入连批次自动锁定；微信粘贴的加粗/倾斜修复；选区主题色。**

- 优化：关联行按标题识别出学生后，继续用标题里的单元进度（U7L1 / U7Day3&U8A Preview）在该班批次里唯一命中自动选批次；粘贴一篇即自动对齐「班级 + 学生 + 批次」，全程只需粘贴
- 修复：微信笔记复制过来的加粗/倾斜不呈现——微信把格式写在内联样式（font-weight/font-style）而不是 b/i 标签上，粘贴清洗时一直当普通文本剥掉了；现在识别内联样式的加粗/倾斜/高亮背景并转成对应格式
- 新增：全站选中文字时的底色换为主题色（浅青底），替代浏览器默认蓝底

## 0.12.0 — 2026-09-19

**新功能：班级排行榜（实时/历次双层）、学生名次走势；撒花庆祝；导入自动锁定学生；移除批改建笔记联动。**

- 班级统计页新增排行榜：「实时排名」含正在批改的批次（ACM 式，没批到的按缺交拉低）；「历次排名」只算全部改完的批次，可选「截至第 N 次」回看当时排名，并有「含提交率 / 纯分数」两种口径切换——含提交率 = 平均分 × 提交率（交一次 100 分压不过三次 99），纯分数 = 有效作业平均分；前三名领奖台（金银铜奖牌 + 台阶底座），同分不设并列、先批完的学生在前，点名字进学生详情
- 学生详情页新增「班级排名 第 X / N 名」与名次走势折线（每次作业后的班内位置变化）
- 批改完一个班最后一份作业的瞬间：全屏撒花 +「全班批改完成，老师辛苦了」弹窗（标准批改与快捷批改都会触发），可一键回到批次页
- 批量导入「关联学生」行也能自动识别了：粘贴内容后按标题锁定学生——练习反馈→厚少班、伴学手册反馈→厚中班，名字唯一命中就自动选好班级和学生，真正「只粘贴就完事」；同名学生不猜、手动改过的选择不会被覆盖
- 批改保存不再自动建/更新笔记：笔记库与批改完全解耦，所有笔记由你手动新建或批量导入（已有的关联笔记保留不动）

## 0.11.1 — 2026-09-19

**补丁：归档名字自动提取 + 数据面板图片体积与状态彩色化。**

- 优化：归档笔记的名字不用再手敲——标题首行按「名字 + 单元号 + 反馈类型」的固定格式自动识别并预填；批量导入大表和笔记详情的归档弹窗都生效；首行是问候语等正文时不会误抽；手动改过的名字不会被自动覆盖
- 优化：管理后台「数据」顶卡新增「图片存储（COS）」（总体积 + 张数，主题色突出显示）
- 优化：数据面板状态彩色化——桶内对象与库内追踪不一致标黄、有孤儿图片待清理标黄、新增「最新备份」状态行按新鲜度绿/黄/红（超 24 小时提醒留意自动备份）、保留策略关键数字着色
- 优化：工作台「待办」点击改为进入批次详情页（可看提交状态、按需选快捷批改或标准批改），不再一点就跳进批改页

## 0.11.0 — 2026-09-19

**新功能：预习答题卡 + 批改联动预习错题。**

- 带预习的批次新增「预习答案」录入：答题卡式登记 5 道预习题的 A/B/C/D（纯对答案用，不算分、不出解析、不走 AI），批次详情页独立入口随录随改
- 常规批改与快捷批改新增预习区：题号连着答案直接显示，点错即登记；快捷批改标错矩阵带 5 列预习格；预习不计入分数
- 勾选预习错题自动勾上「预习有错题」话术并填好「错了 N 个小题」，可手动改数、勾选变化自动回落；全部取消勾选则话术自动撤下
- 关闭批次的预习开关会清空已录答题卡与全体学生的预习错题记录（编辑批次时有提示）
- 修复：本地开发时广告拦截插件误拦 fingerprint.js（文件名触发指纹追踪规则）导致白屏，已改名 contentfp.js
- 修复：批改台与快捷批改补上防丢弹窗——有未保存改动（含预习勾选）时返回/跳转/刷新先弹确认；此前全站只有笔记编辑页有该保护，批改页的脏标记一直只驱动状态灯（碳碳记忆中的「批改页返回弹窗」从未存在过）

## 0.10.0 — 2026-09-19

**新功能：评级分数线可视化 + 孤儿图片清理。**

- 管理后台「评级」页：十二档分数线可视化调整（范围/严格递减/F 固定 0 三重校验、恢复默认一键回）；保存后批改预览与落库即刻按新线执行，历史已存等级不回溯
- 管理后台「数据」新增未引用图片清理：扫描预览（个数/体积）→ 二次确认真删；只清 7 天前上传且任何笔记都未引用的 COS 对象；清理后同步回收库内追踪记录

## 0.9.0 — 2026-09-19

**新功能：全站备份接入对象存储；批量导入与搜索体验优化。**

- 数据库一致性快照（sqlite .backup）上传 COS backups/ 目录，异地留存；每日自动备份（最新超 24 小时自动补），保留最近 30 份自动清理
- 管理后台「数据」新增备份区：备份列表（时间/大小）、立即备份按钮、预签名下载；恢复为手动操作（下载后停服替换数据库重启）
- 优化：批量导入续行继承上一行的模式（归档连着录一直是归档，切成关联后续行跟随）；手动「添加一行」同样跟随末行
- 优化：笔记库搜索结果主题色高亮命中关键词（标题/摘要/关联行，大小写不敏感）

## 0.8.2 — 2026-09-19

**补丁：批量导入自动续行 + 状态灯胶囊化。**

- 优化：批量导入大表最后一行一旦开始填写（打字/粘贴/选关联），自动在末尾补一行空行，连续录入不再手动点「添加一行」；末尾空行显示为虚线占位（提交时自动跳过），删光行时保底留一行
- 优化：保存状态灯改为胶囊条并固定宽度，「已同步/有未保存修改/保存失败」切换时不再撑动版面

## 0.8.1 — 2026-09-19

**补丁：修复 AI 录题长文档必败与日志上报断连噪音。**

- 修复：AI 录题在长文档时必然失败——边缘代理约 60 秒无响应即掐断长连接（524）；拆题改为任务制 + 轮询，「已整理 N 题」进度显示不变，长文档随便跑
- 修复：页面关闭瞬间的客户端日志上报被浏览器掐断时，服务端日志出现 EndOfStream 堆栈刷屏（现识别为正常断连，安静处理）

## 0.8.0 — 2026-09-19

**新功能：笔记批量导入、归档区、保存状态灯。**

- 笔记批量导入：一张大表一次录入多篇，内容区支持直接粘贴图文（图片自动上传）；每行可选「关联学生」（落活跃区）或「历史归档」（落归档区，可填学生名备注）
- 归档区：笔记详情可手动归档/还原，归档可填学生名备注并支持搜索；旧班数据不再出现在活跃列表
- 保存状态灯：笔记编辑、批改台、快捷批改的保存按钮旁常驻状态灯（已同步 / 有未保存修改 / 保存失败），与既有防丢弹窗互为冗余
- 修复：批次短码链接下 AI 录题报「[object Object]」（URL 短码被当数字 id 提交触发 422；改为取批次数字主键，错误文案统一归一为可读文本）

## 0.7.3 — 2026-09-18

**补丁：修复旧笔记贴图丢失与话术保存报错，笔记标题可手动编辑。**

- 修复：旧笔记粘贴图片保存后丢失（粘贴内容落为编辑器顶层裸节点时，图片不随序列化入库；新建笔记不受影响所以此前未暴露）
- 修复：一次粘贴多张图片时落地顺序颠倒（insertNode 不移动光标，后插入的顶到先插入的前面；现插图后光标移到图片之后）
- 修复：话术管理保存弹「clientLog is not defined」（0.7.2 指纹日志漏引模块；PUT 实际成功，话术未丢）
- 笔记标题：自动剥除首行格式标记（不再出现 ** 星号）；编辑态标题可手动修改，不改动时仍按首行自动取
- 笔记列表摘要中的图片标记显示为 [图片]，不再显示误导性的「图片未加载」

## 0.7.2 — 2026-09-18

**补丁：笔记三处修复 + 日志体系六项增强，诊断包能真正定位问题了。**

- 修复：笔记保存后新贴图误显「图片未加载」（改为保存后整笔记重拉）；「图片未加载」占位再次保存永久丢图（占位保留 data-key 往返还原）；0.7.1 前存入的多余空行保存时自愈坍缩
- toast 全量入日志：antd message 经 MutationObserver 采集，无需改调用点
- 客户端事件落库：clientLog 定时批量上报 SQLite，诊断导出同时带落库段与本地未 flush 尾段
- 服务端留痕：5xx 与慢请求（>3s）进环形缓冲，启动事件带版本号
- 关键操作内容指纹：笔记保存、话术保存、批改保存带 len/img/fp（djb2），诊断对比无需原文
- JS 错误记完整堆栈，资源加载错误单独记录；前端构建版本与构建时间注入诊断包

## 0.7.1 — 2026-09-16

**补丁：修复粘贴图片保存后丢失与粘贴段落空行。**

- 修复粘贴图片保存后丢失：插图原实现在编辑器失焦时静默失败，改为与焦点无关的插入方式（光标处或自动追加到末尾），并记录落地路径供诊断
- 修复微信笔记粘贴后段落间出现多余空行（HTML 空块不再被转换成换行）

## 0.7.0 — 2026-09-15

**笔记排版三件套 + 话术管理界面 + ESA AI 验证码接入，更新弹窗修复。**

- 笔记编辑器排版：加粗/倾斜/高亮工具栏；白名单内联标记存储（保存后格式不丢）；清洗式粘贴保留微信笔记的格式；阅读态与复制全文同步支持排版
- 话术管理：管理后台可在线编辑任意话术（含内置）内容 + 格式开关（标题加粗/正文倾斜），格式直通反馈预览与复制；支持新增自定义话术
- ESA AI 验证码：登录页发码与密码登录接入阿里云无痕/拼图验证（边缘验签），自托管图形验证码保留为开发环境回退
- 修复：更新弹窗补丁版本不弹（取补丁顺序错误）

## 0.6.3 — 2026-09-13

**补丁：全站时间统一北京时间；Windows 微信迁移补救与拖拽上传。**

- 全站时间戳统一按 UTC+8（Asia/Shanghai）显示：管理后台 AI 调用/邀请码、笔记更新时间与列表日期
- Windows 微信剪贴板不含图片数据时明确提示；编辑器新增拖拽图片上传（悬停虚线框反馈）

## 0.6.2 — 2026-09-13

**补丁：图片可取回，迁移进度可视化。**

- 阅读态点击图片新开标签页看原图（浏览器原生复制/另存）；「下载全部图片」按正文顺序打包逐个下载
- 图片迁移进度条：编辑器顶部主题色细条随第几张实时推进，完成淡出；底部文字提示保留
- 签名 URL 过期有提示，刷新即重签

## 0.6.1 — 2026-09-13

**补丁：笔记详情修复与体验收尾，编辑器防丢保护。**

- 修复：笔记点进去空白（数据加载时序问题）；详情默认阅读模式，编辑为主动动作，复制全文一键可取
- 微信笔记整篇迁移：粘贴时解析文中图片自动迁移（data URI 直传 / 外链后端代取，带 SSRF 防护），拿不到的保留占位提示手动补
- 图片限高 480px；笔记列表摘要保留换行、卡片三层排版
- 编辑器防丢：有改动时离开页面/刷新/取消三路确认

## 0.6.0 — 2026-09-13

**笔记库：图文混排反馈存档，对象存储直传直读，批改联动单向同步，可搜可筛可回溯。**

- 笔记库新页面：工作台入口，列表支持搜索（学生名/标题）、班级筛选、时间范围筛选，每条笔记显示图片数与大小，顶部汇总总用量
- 所见即所得编辑器：粘贴图片自动上传（COS 预签名直传，不经过服务器）、图文混排、标题自动取首行
- 双模态：在带班级可关联班级/学生/批次；历史班级游离笔记直接录入
- 批改联动：保存批改自动生成关联笔记（单向同步；手动编辑过的笔记永久脱钩不被覆盖）
- 对象存储：腾讯 COS 私有桶 + 签名 URL 限时读取，类型白名单 + 大小上限，删笔记连带清对象
- 管理后台：数据面板新增对象存储区块（配置状态、库内追踪、桶内对照）
- 协议更新：用户协议与隐私政策补充笔记库、COS 存储、学生照片保护条款
- 诊断：笔记与图片计数、笔记操作前端留痕

## 0.5.1 — 2026-09-13

**补丁：批次对外短码（不可枚举 URL）；用户协议与隐私政策全文重写；诊断日志补充状态分布。**

- 批次 URL 改用 8 位不可枚举短码（去易混淆字符、防手误），数字编号入口保留兼容，存量批次启动自愈回填
- 用户协议与隐私政策全文重写为大厂风格（共 26 章）：覆盖账号与邀请码制、AI 功能免责、第三方服务披露（DeepSeek/Resend/Zeabur/DiceBear）、学生信息特别约定、诊断日志说明、数据权属与备份删除权利
- 诊断日志：提交状态分布计数、AI 调用按功能分类计数、协议版本与启动时刻入库
- 界面：设置页箭头改 SVG 图标

## 0.5.0 — 2026-09-12

**题库纠错闭环 + 快捷批改防录错：题号照抄原文、板块顺序可调、整批编辑与一键清空，批改矩阵十字准线；版本日志改版并新增更新与协议变更提醒。**

- AI 拆题题号照抄试卷原文：seq 逐字使用原文真实题号，Vocabulary A 1-5、B 6-8 连续编号不再被重排成 1-3；仅原文完全没有题号时才板块内从 1 编
- 板块顺序手动可调：题库目录三横线手柄拖拽或上下移排序，批改勾选器、快捷批改矩阵、反馈文本全站跟随；默认顺序由字典序改为录入顺序（历史乱序自愈）；重命名板块联动排序配置
- 整批编辑：独立编辑页回到录题验收界面一次改全库，带 id 行更新保引用（错题记录、批改提交、反馈快照不受影响）、无 id 行新增、消失行删除并级联清理对应错题
- 一键清空题库：双重确认（列明题目/错题/批改/快照四类数据条数 + 输入「清空」二字），清空后批次保留、回到刚建状态
- 录题追加语义显性化：AI 录题与手工录题弹窗均提示「已有 N 题，本次追加不覆盖」
- 快捷批改矩阵：悬停十字准线（整行整列高亮 + 交点强调，防串行串列）、等级独立胶囊列随分档变色（列序：分数｜等级｜保存）、点学生名直达标准批改、板块间分组留缝
- 修复：答案速查区长答案断行不再撑破页面；矩阵 border-collapse 改 separate 修复 Chrome sticky 边框丢失、表头竖向钉顶（竖滚时题号常驻）
- 内部编号 #id 显示层：批次页 meta 与批改页学生信息栏显示批次 #id、题卡题号徽章旁显示题目 #id，点击复制
- 版本日志改版：数据源升级为 minor/patch 嵌套结构，设置页改折叠卡片（最新版默认展开、补丁嵌套在所属版本内）；登录后自动弹「更新啦」提醒（每版本一次）；《用户协议》《隐私政策》修订后自动弹变更提醒并直达查看

## 0.4.1 — 2026-09-08

**补丁：反馈渲染三态合一，复制到微信笔记保留加粗；GZip + 缓存头修复线上加载缓慢。**

- 预览 / 纯文本 / HTML 收拢为单一数据源，修复板块标题多出【】、空行不一致等货不对板
- 「复制反馈」双格式（纯文本 + HTML），粘贴进微信笔记保留加粗；板块名已含「部分」不再重复追加
- 快照视图默认结构化渲染（保存前后零变化），补录快照回退纯文本并折叠空行
- 正确率胶囊改版：「正确率」标签 + 收紧居中
- 性能：GZip 压缩（JS 1.8MB → 0.53MB）+ 静态资源 immutable 缓存 + SPA 路由一律 no-cache，修复线上每次切换白屏约 10 秒

## 0.4.0 — 2026-09-07

**统计可视化版：学生趋势与班级统计开页，题库逐题正确率直达答错名单。**

- 学生详情页：历次作业趋势折线（未交断开不连、数据点按分数着色）、薄弱板块条形图、历次明细可点进批次；班级页点名即达
- 班级统计页：历次平均分趋势（含提交人数）、提交情况堆叠条、每题错误排行（哪题错得多下次课该讲什么）
- 题库逐题正确率：每题旁「正确率」胶囊（两位小数、三档配色），点击弹答错学生名单、点名直达批改页定位
- 批改体验：快照不再混入问候语（存量快照展示时自动剥离）；新增「清空勾选」（二次确认）；状态改回「待批改」保存即完整复位批改记录
- 反馈格式：复制全部改为「复制反馈」（纯正文）；客观题带答案（题号+答案行、解析原文照抄）；验收表格新增「解析：独立成行」开关
- 诊断冗余：页面浏览与关键操作留痕进诊断包；诊断数据段补题库/错题/快照计数；SOP 新增诊断导出检查条目

## 0.3.0 — 2026-09-07

**快捷批改版：答案速查一屏看全，标错矩阵只管录入；补录与快照体验闭环。**

- 快捷批改（新页面）：答案速查区（按板块分组、纯答案紧凑网格、长短答案自适应排版）+ 学生×题目标错矩阵（点格子标错、行尾实时分数、每行可存/全部保存只存改动行、分数与保存列右侧常驻）；notes 透传、快照不覆盖、状态语义沿用
- 快照回显：已保存学生预览区直接显示存档原文（「已存档快照」标记），任何表单编辑自动切回实时预览；补录落库即刻可见
- 新建批次表单：日历选日期、时间半小时步进下拉、作业内容下拉（随课型联动）
- AI 录题：prompt 加固「解析：」等字段前缀必须保留；验收表格新增一键补「解析：」前缀
- 修复：重发验证码的人机验证改倒计时结束后才展开；提交状态列表等级列定宽对齐；登录页 useNavigate 缺失崩溃；作业内容下拉随课型联动

## 0.2.0 — 2026-09-07

**账号体系上线 + 题库体验完整化 + 上线前安全加固，Autograde 正式具备公网部署条件。**

- 账号体系：邮箱验证码 / 密码 / UID 三种登录方式（UID 输入后验证码发往绑定邮箱，全程防枚举），注册五步流（邮箱→验证码→头像→资料→成功页展示 UID），邀请码制注册，JWT 会话（7 天）
- 多租户隔离：一切业务数据以班级归属人为锚点，越权访问一律 404；首个注册用户自动成为管理员并接管存量数据
- 管理后台：总览 / AI 用量 / 话术 / 邀请码 / 安全 / 数据（备份下载）六个面板
- 题库体验：AI 录题流式进度（实时显示已整理题数）、解析换行保留、选项单独抽取、题库按 Task 目录导航、板块一键重命名、题目卡片式排版（题干/选项/答案分层，答案主题色高亮）
- 批改体验：Issue 情况话术勾选入库（含预习错题数选择）、问候语按时段匹配可手选可换一条、十二档等级分数线（A+ 至 F）、分数颜色策略（≥75 绿 / 60-75 橙 / <60 红）、未交与缺项计入批改进度、页面切换动画、弹窗居中、可点击面包屑
- 班级与学生管理：新建/编辑/删除班级（删除需双重确认 + 输全名）、名单批量导入、学生备注
- 安全加固：修复 SPA 路径穿越漏洞、诊断导出全面脱敏（密钥只报布尔、邀请码只报统计、URL 打码）、登录类接口防枚举统一文案、注册邮箱归一化、限流取真实来源 IP、AI 接口用量护栏
- 设置页：版本日志与开源声明板块；LICENSE 采用 Apache 2.0 + Commons Clause（源码可见、禁止商用销售）

## 0.1.0 — 2026-09-06

**从只读演示走向可真实批改：反馈标题按两班规则自动拼装，批次/题库/学生写接口全开，批改落库，AI 录题代码就绪。**

- 反馈标题规则结构化落地：厚少（WW）`{学生} U{n}L{m} 练习反馈`，厚中（NG）`{学生} U{n}Day{m} 伴学手册反馈`，含预习追加 `&U{n}{A|B} Preview`；批次单元进度改为结构化字段，单元号按册范围自动校验
- 后端写接口：学生 CRUD、批次 CRUD（课型按班级系列预填）、题库批量冻结与单题增改、批改落库（分数/等级后端复算，错题记录与反馈快照事务写入）
- AI 接入（OpenAI 兼容，默认 DeepSeek deepseek-v4-pro）：`POST /api/ai/parse-questions` 粘贴文本拆分题库草稿（含水印剔除、赋分预填，不落库）、`POST /api/ai/draft-explanation` 错点描述起草讲解；key 未配置时优雅降级 503
- 前端：批次创建/编辑 modal（预习开关仅 Day 课型显示）、手工录题与 AI 录题双入口（共用可编辑验收表格）、批改页统一编辑窗按题目模式分态、「保存批改」落库后状态灯/分数由后端驱动
- 站标「Autograde」字号放大（--t-display 22px，衬线不变）
- 工程化：GitHub 公开仓库开通；DEVELOPMENT.md / AGENTS.md 转为本地文档不进公开仓库（对齐 Stellaris 惯例）

## 0.0.1 — 2026-09-06

**工程骨架落地：FastAPI + SQLite 后端九表建模，React + AntD 前端四级页面贯通，演示数据开箱即用。**

- 后端：9 张表全量建模（classes / students / assignments / questions / error_records / submissions / feedback_snapshots / phrases / settings），`_ensure_columns` 启动自愈
- 后端：访问口令中间件（ACCESS_TOKEN）、CORS 放行 Vite 5173、只读接口覆盖四级页面
- 演示数据：厚少 WW5A（8 名学生）+ 厚中 NG3B（6 名学生），U7Day1 批次 4 个 Task 共 20 题（verbatim / ai_expand / manual 三种 mode），提交四态示例齐全
- 前端：tokens.css 1:1 落地并映射 AntD ConfigProvider 主题（colorPrimary #0d9aa7、圆角 6、三字体栈）
- 前端：工作台 / 班级 / 批次 / 批改四个路由页，批改页三栏布局复刻概念稿，勾选芯片按 score_weight 实时算分（两位小数），十档等级自动预选 + 手动覆盖
- 勾选状态仅存前端内存，不落库；不接任何 AI 调用
